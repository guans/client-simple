package com.ubirtls.test;

import java.io.IOException;
import java.net.InetAddress;
import java.net.Socket;
import java.net.UnknownHostException;

import com.ubirtls.config.Setting;
import com.ubirtls.event.AuthenticationEvent;

import android.app.Activity;
import android.os.Bundle;
import android.util.Log;
import android.widget.TextView;

public class SocketTest extends Activity {

	/*
	 * private static InetAddress host; private static final int PORT = 1234;
	 * private TextView tv = null; private ServiceEngine serviceEngine = null;
	 * 
	 * @Override public void onCreate(Bundle savedInstanceState) {
	 * super.onCreate(savedInstanceState); tv =
	 * (TextView)this.findViewById(R.id.tv); setContentView(R.layout.main); //
	 * String text = new Setting().toString(); // Log.i("tag", text); try { host
	 * = InetAddress.getLocalHost(); } catch(UnknownHostException uhEx) {
	 * System.out.println("Host ID not found!"); System.exit(1); } Socket link =
	 * null; //Step 1.
	 * 
	 * try { link = new Socket(host,PORT); serviceEngine = new
	 * ServiceEngine(link, "first"); serviceEngine.login("hu", "xuke"); } catch
	 * (IOException e) { // TODO Auto-generated catch block e.printStackTrace();
	 * } //Step 1.
	 * 
	 * //tv.setText(text); }
	 * 
	 * @Override public void ServiceEngineActionPreformed(ServiceEngineEvent
	 * event) { // TODO Auto-generated method stub if(event instanceof
	 * AuthenticationEvent) { tv.setText("login return"); } }
	 */
}
